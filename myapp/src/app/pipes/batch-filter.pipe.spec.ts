import { BatchFilterPipe } from './batch-filter.pipe';

describe('BatchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BatchFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
