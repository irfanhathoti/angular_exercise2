import { ComponentInteractionPipe } from './component-interaction.pipe';

describe('ComponentInteractionPipe', () => {
  it('create an instance', () => {
    const pipe = new ComponentInteractionPipe();
    expect(pipe).toBeTruthy();
  });
});
